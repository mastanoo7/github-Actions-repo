name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
    build:
        runs_on: ubuntu-latest
        steps:
            -name: build
             run: echo "Building the project..."
    test:
        runs_on: ubuntu-latest
        steps:
            -name: test
             run: echo "Running tests..."