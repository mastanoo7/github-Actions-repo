name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo "Building the project..."

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "Running tests..."